# Point ES_PATH_CONFIG to where you place this file
# /etc/elasticsearch/elasticsearch.yml default for Debian

path:
  data: /var/lib/elasticsearch
  logs: /var/log/elasticsearch

http.port: 9200
node.name: ${HOSTNAME}
network.host: 
